# =============================================================================
# AMIGO - Worker secrets (names only)
# =============================================================================
# Manifest for wrangler deploy --secrets-file. Values come from process.env
# (injected by op run from a 1Password Environment). Same keys belong in
# amigo (dev) and amigo (prod) with environment-appropriate values.
# =============================================================================

CLERK_SECRET_KEY=
VAPID_SUBJECT=
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
